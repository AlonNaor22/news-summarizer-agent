server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # SPA: hand any unknown path to index.html so react-router can take over.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
