# Core dependencies — pins match pyproject.toml to avoid LangChain breaking changes.
# Prefer `pip install -e .[dev]` (pyproject.toml) as the canonical install path.

langchain>=0.3.0,<0.4.0
langchain-anthropic>=0.3.0,<0.4.0
anthropic>=0.40.0
feedparser>=6.0.10
requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
python-dateutil>=2.8.0
tenacity>=8.2.0
